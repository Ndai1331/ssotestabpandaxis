@page "/user-signatures"
@attribute [Authorize]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() =>
        Navigation.NavigateTo("/account?tab=signatures", replace: true);
}
